package com.evcc.booking.enums;

/**
 * Enum đại diện cho loại đặt lịch
 */
public enum BookingType {
    ONE_TIME,       // Đặt lịch một lần
    RECURRING       // Đặt lịch lặp lại (hàng ngày, hàng tuần, hàng tháng)
}